# SAP Connection (same as mcp-abap-abap-adt-api)
SAP_URL=https://your-sap-host:44300
SAP_USER=YOUR_USER
SAP_PASSWORD=your_password
SAP_CLIENT=100
SAP_LANGUAGE=EN

# LLM for AI Chat — use Groq (free) or OpenAI (if Groq is blocked)
# Option A: Groq (free at https://console.groq.com). Set LLM_PROVIDER=groq or leave unset.
LLM_PROVIDER=groq
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=llama-3.3-70b-versatile

# Option B: If Groq is unreachable (firewall/proxy), use OpenAI instead:
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-your-openai-key
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_API_BASE=   (optional, e.g. for Azure or OpenRouter)

# Local CDS view list (optional; used for dropdown)
CDS_SOURCE_PATH=../zanalytics-cds

# Server
PORT=4000
